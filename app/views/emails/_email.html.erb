<div class="px-4">
  <div class="py-2 text-lg border-b border-gray-900">
    <%= @email.body %>
  </div>
  <div class="py-4 text-sm text-gray-500">
    <% if @email.outbound? %>
      Sent by <%= @email.user.email %> on <%= l(@email.created_at, format: :long) %>
    <% else %>
      <div class="flex items-center justify-between">
        <span>
          Sent by <%= @email.applicant.name %> on <%= l(@email.created_at, format: :long) %>
        </span>
        <span>
          <%= link_to "Reply",
            new_applicant_email_reply_path(
              applicant_id: @email.applicant_id,
              email_id: @email.id
            ),
            class: "btn-primary-outline",
            data: {
              remote: true
            }
          %>
        </span>
      </div>
    <% end %>
  </div>
</div>